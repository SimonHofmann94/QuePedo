// Legacy re-export — use `@chingon/shared/design/tokens` for the full system.
// Kept for backwards compatibility with any old imports.
export { colors as palette, semantic } from '../design/tokens'
